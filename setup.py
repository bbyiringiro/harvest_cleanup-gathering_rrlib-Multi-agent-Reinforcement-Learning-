from setuptools import setup, find_packages

setup(
   name='ssd',
   version='1.0',
   description='rL agent',
   author='Billy Byiringiro',
   author_email='',
   packages=find_packages(), #['fruit_gathering'],  #same as name
#    package_dir=['..'],
   install_requires=[], #external packages as dependencies
)